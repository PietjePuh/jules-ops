# Dispatch triage — 24/09/2026 21:10 UTC

Written because the Paperclip control plane refused issue writes this heartbeat
(`cross_issue_influence_run_context_required` — the wake carried no
`PAPERCLIP_TASK_ID`, so every issue comment/PATCH was rejected as a cross-issue
write). This file is the durable record instead. It belongs on
[TIM-25](/TIM/issues/TIM-25), [TIM-13](/TIM/issues/TIM-13),
[TIM-22](/TIM/issues/TIM-22) and [TIM-18](/TIM/issues/TIM-18).

---

## 1. Session ledger — state at exit

| Session | Repo | State @ 21:10 UTC | What I did this pass |
| --- | --- | --- | --- |
| `5773481294591418578` | Toolbelt | `IN_PROGRESS` | Left alone. Briefed at 20:19 on 5 root causes, actively committing (last artifact 21:06). |
| `14586316274894910497` | Toolbelt | `QUEUED` (**started this pass, 21:10**) | New. Stale-test retirement; supersedes PR #4097. |
| `1861008097792993223` | Toolbelt | `AWAITING_USER_FEEDBACK` → messaged 21:07 | Unstuck. It asked whether to edit `extension-rpc.test.js/.mjs`. Answered: **no** — those tests are correct and its own diff is the regression. |
| `16733254232003562143` | omarchy-toolbelt | `AWAITING_USER_FEEDBACK` → messaged 21:08 | Unstuck. Told to finish, run the suite, open the PR, and report pre-existing failures rather than paper over them. Its draft is PR #21. |
| `17699472820348520230` | dotfiles | `COMPLETED` | Produced dotfiles PR #1 (draft). |

Older `AWAITING_USER_FEEDBACK` sessions from 23/09 (`669221713274487286`,
`4991982065234255366`, `17270558410072486918`) are stale persona runs; they are
`jules-stalled.sh`'s to sweep, not worth a slot.

### The correction sent to `1861008097792993223`

Its brief (`briefs/TIM-18-slice1-bus-envelope.md`, line 19) says: keep
`invokeExtension` "working **unchanged** for every existing caller ... map the
response back to **today's** `{ ok, data }` / `{ ok: false, error: <string> }`
shape." Jules instead changed the error strings to `extension invoke: TIMEOUT`
etc., which reddened `extension-rpc.test.js/.mjs`, and then asked whether it
should edit those tests. It must not. The wrapper has to translate the v1 error
envelope back to the exact legacy strings; `invokeBus` keeps the structured
form. Sent with the exact mapping and a `git diff --stat` check so the two test
files are provably untouched.

---

## 2. Toolbelt `main` — complete failure map

Source: full log of CI run `36025835021`, main @ `b0daf9d1`. Every failing
assertion, grouped by root cause, with owner.

| # | Cluster | Tests | Owner |
| --- | --- | --- | --- |
| 1 | `offscreen-llm-manager.js` bare listener — trips bare-listener ratchet (39 > 38), `MODULE_LISTENERS`, sender-blind ratchet, **and** steals the first captured listener from `writing-companion` (25 tests) | 28 | `5773481294591418578` |
| 2 | SSRF allowlist line drift — `onboarding/setup.js:169` → `:170` | 2 | `5773481294591418578` |
| 3 | WebGPU route lost its `-MLC` model suggestions (feature regression) | 1 | `5773481294591418578` |
| 4 | `ai_hub_tools` origin match fails on trailing slash | 1 | `5773481294591418578` |
| 5 | `ci-regression-harness` — cascade only, no separate fix | 1 | `5773481294591418578` |
| 6 | Stale tests for features removed in `74f702e3` + `package.json` version drift | 8 | `14586316274894910497` (**new**) |
| 7 | `pentestScopeList` anchor not found | 4 | **unowned** |
| 8 | sidepanel: #702 inline styles (12), #703 loading text, All Tools tab, `settings-patch-serialization`, `runPageTool` | 5 | **unowned** |
| 9 | `GET /api/fleet` returns 500, expected 200 | 1 | **unowned** |
| 10 | Reachability STRICT: 2 orphans (`backup-manager.js`, `pentest-policy.js`) | lint job | **unowned** ([TIM-22](/TIM/issues/TIM-22), brief written) |

Clusters 1–6 landing turns the test shards green. Clusters 7–9 must land too,
and cluster 10 reds `Extension - Lint, Test & Package` independently of any test.

### Cluster 6 — why PR #4097 cannot turn main green alone

`74f702e3` ("Remove Pipelines + playbook checklist + fake ESP32 UI") deliberately
deleted `background/modules/pipeline-executor.js` and
`content/esp32-controller.js`. Verified: both MISSING on `origin/main`. The stale
tests `readFileSync` those paths and die with `ERR_TEST_FAILURE`/ENOENT.

#4097 does the version bump, deletes the five esp32/word-counter/subdomain test
files, and drops `modules/pipeline-executor.js` from `FILE_ALLOWLIST` — all
correct. It **misses** `tests/install-handler.test.mjs:266`
(`initDefaultPipelines: seeds 4 default pipelines` → `'toolbelt_pipelines seeded'`),
which tests the same removed feature. That omission alone keeps main red.

Recommendation to Tim: when `14586316274894910497`'s PR is green, merge it and
close #4097. **I do not merge.**

---

## 3. Ready-to-merge queue

Merged in Toolbelt since 18/09: **14**.

### Toolbelt — 9 open, all carrying `no-auto-merge`

The brake is fully on: every open PR is labelled, so turning CI green no longer
auto-lands anything. This satisfies the sequencing constraint on
[TIM-13](/TIM/issues/TIM-13).

| PR | Verdict |
| --- | --- |
| #4098 | Retires the dead auto-merge schedule path, makes the event path opt-in. **Land this first** — it is the permanent brake. Red only from main. |
| #4097 | Partial CI fix. Superseded by session `14586316274894910497`. Hold, then close. |
| #4096 | Bolt: N+1 in document pruning. Otherwise clean; red only from main. |
| #4095 | Palette: confirm dialog on memory deletion. Otherwise clean; red only from main. |
| #4094 | Sentinel: SSRF + scope bypass in `sw-auditor`. Security fix — wants a real read before merge. |
| #4091 | Draft, "verify sw-auditor SSRF fix already present" — likely redundant with #4094. Close candidate. |
| #4090 | Sentinel: MCP Firewall sender authz + rescan. Security fix — wants a real read. |
| #4089 | Concurrent execution for `responseContain`. Red only from main. |
| #4078 | Palette: Flipper Toolkit delete confirm. `DIRTY` — needs a rebase before it is judgeable. |

Nothing here is merge-ready while main is red, except by Tim's explicit choice.

### omarchy-toolbelt — 2 open

| PR | Verdict |
| --- | --- |
| #22 | `CLEAN`, Tim's own — gc guard test fixture + gcal gating. **Genuinely ready to merge now.** The only green one across all three surfaces. |
| #21 | Jules draft, Python suite runner + CI. `UNSTABLE`. Nudged 21:08 to finish and undraft. |

### dotfiles — 2 open, both draft

| PR | Verdict |
| --- | --- |
| #2 | Tim's `install.sh` one-command fresh-machine installer. `CLEAN` but draft. |
| #1 | Jules: skipped-step tracking + `--strict` in `omarchy-bootstrap`. `CLEAN` but draft. Session COMPLETED, so this is ready to undraft and judge. |

---

## 4. Finding: the v2 surface freeze has no CI half

My instructions and `DISPATCH-RANKING.md` both state the freeze is enforced by
`tests/regression/v2-surface-freeze-ratchet.test.js`, and every Toolbelt brief
repeats verbatim that "CI enforces this ... and your PR will be red."

**That is currently false.** Neither `tests/regression/v2-surface-freeze-ratchet.test.js`
nor `tests/baselines/v2-surface-freeze-baseline.json` exists in `Toolbelt` on any
branch. The freeze is enforced only by the dispatch half — my briefs. Jules can
add surface today and nothing stops it.

Two gaps, both now visible rather than silent:

1. `DISPATCH-RANKING.md` was committed to `v2-surface-freeze-dispatch` and pushed,
   but never had a PR, so it is not on `jules-ops` `main` either. Opened as
   [jules-ops#2](https://github.com/PietjePuh/jules-ops/pull/2) this pass, with the
   CI gap called out in the PR body rather than buried.
2. The ratchet test itself needs building. That is rank-2 work (a gate that makes
   a freeze rule harder to evade) and is Jules-sized and self-verifiable. Queued
   as the next Toolbelt brief — not dispatched, because two Toolbelt sessions are
   already live.

Until the ratchet lands, the freeze paragraph in each brief overclaims. I am
leaving the text verbatim as `DISPATCH-RANKING.md` mandates rather than quietly
editing it, but this is the reason it is not yet true.

---

## 5. Next actions, owned

- **Me, next wake:** record `14586316274894910497` and `5773481294591418578`
  states; sweep `1861008097792993223` and `16733254232003562143` for whether the
  corrections took; dispatch cluster 7/8/9 briefs as Toolbelt slots free.
- **Me, next wake:** write the `v2-surface-freeze-ratchet.test.js` brief.
- **Tim:** omarchy-toolbelt #22 is green and ready. Toolbelt #4098 is the one to
  land first once main is green.
- **Blocked on nothing.** No session is waiting on a human.
