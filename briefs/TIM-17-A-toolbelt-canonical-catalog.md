Repo: PietjePuh/Toolbelt. Branch from main. Create ONE branch and ONE PR.

Do not ask any questions. Every decision you need is below. If something is
genuinely undecidable, pick the option stated as "default" and say so in the PR
description.

## SURFACE FREEZE (active, this repo)

The v2 plan is a subtraction plan, so new surface is not dispatched at all: no
new `*-hub/` directory or standalone hub page, no new `background/modules/`
engine polling an upstream an `omarchy-toolbelt/bin/*.py` engine already owns,
no `sidepanel/tools-catalog.json` entry without a host-catalog counterpart.

You may NOT self-issue a `// ratchet-ok: <reason>` or `"ratchetOk"` escape hatch
under any circumstances. If you believe you need one, stop and explain why in
the PR description instead of adding it. This task adds ZERO tool entries — the
browser catalog must come out of this PR byte-identical to how it went in.

## Context: the bug you are fixing

There are two hand-synced tool catalogs and they have drifted:

- `Toolbelt/sidepanel/tools-catalog.json` — 198 entries (this repo).
- `omarchy-toolbelt/catalog/tools.json` — 203 entries (the Omarchy desktop bar).

`scripts/sync-tools-catalog.cjs` only ever INSERTS new pages. It has no removal
path, which is why the desktop copy still lists 12 pages that were deleted from
this repo. Your job is the Toolbelt half: emit ONE canonical generated artifact
plus a stable content hash, and gate it in CI so drift is a red build.

The omarchy-toolbelt half is a separate PR. Do NOT touch that repo.

## Measured facts (verified against main on 2026-09-24 — trust these, do not re-derive)

    browser catalog      198 = 194 with a `path` + 4 external-URL entries (no `path`)
    desktop catalog      203 = 191 live paths + 12 dead paths
    shared paths         190
    browser-only paths     4   devtools/devtools.html, sidepanel/diagnose-page.html,
                               onboarding/setup.html, tools/file-browser/file-browser.html
                               (all outside a *-hub/ dir, so the desktop generator never saw them)
    desktop-only alive     1   ai-hub/ai-hub.html
    external-URL entries   4   (browser only; no path, so no chrome-extension:// URL exists)

    => CANONICAL = 190 + 4 + 4 + 1 = 199
    => browser view  = 198  (MUST be byte-identical to the current file)
    => desktop view  = 191

`ai-hub/ai-hub.html` is the ONE entry whose two surfaces genuinely disagree: it
sits in the SKIP list in `scripts/hub-pages.cjs` ("ai-hub is itself a launcher
grid") so it is deliberately absent from the browser catalog, but the Omarchy bar
legitimately opens it. That single entry is the entire reason the `surfaces`
field exists. Get it right.

## Deliverable — exactly these 7 files

### 1. `catalog/catalog.schema.json` (new)

JSON Schema (draft 2020-12) for the canonical artifact. Per entry require:
`id` (string), `hub` (string), `title` (string), `path` (string, may be empty
for external-URL entries), `surfaces` (array, non-empty, items enum
`"browser" | "desktop" | "agent"`, uniqueItems), and `entrypoints` (object
keyed by surface name). Top level: `generator`, `hash`, `tools`.

### 2. `catalog/catalog.json` (new, GENERATED)

Shape:

    {
      "$schema": "./catalog.schema.json",
      "generator": "scripts/sync-tools-catalog.cjs",
      "hash": "<sha256 hex>",
      "tools": [ ... 199 entries, sorted by id ... ]
    }

Add a `DO NOT EDIT — generated by scripts/sync-tools-catalog.cjs` note. JSON has
no comments, so carry it as a top-level `"_comment"` string field and declare
that field in the schema.

**Hash definition (use exactly this, the other repo's PR depends on it):**
sha256, lowercase hex, over `JSON.stringify(tools)` where `tools` is sorted
ascending by `id` and every entry object has its keys sorted alphabetically. The
`hash` field itself and `_comment`/`$schema`/`generator` are NOT part of the
hashed input. The check recomputes from `tools` alone and compares.

### 3. `scripts/sync-tools-catalog.cjs` (extend — do NOT write a second generator)

Emit all three artifacts from one scan of the hub pages on disk:
`catalog/catalog.json` (canonical), `sidepanel/tools-catalog.json` (browser
view), and nothing else. Keep the existing category-scoped `insertionIndex()`
behaviour and the existing "curated names/categories never regress" guarantee —
those exist so parallel PRs do not collide, do not regress them.

**Surface derivation — implement exactly this, it is deterministic:**

- Candidate set = every `*-hub/**/*.html` page on disk, MINUS the `SKIP` list
  from `scripts/hub-pages.cjs`, PLUS the non-hub and external-URL entries
  already present in `sidepanel/tools-catalog.json`.
- `browser` ⟸ candidate is not in `SKIP`. (Pathless external-URL entries are
  always `browser`.)
- `desktop` and `agent` ⟸ candidate's path is under one of the `HUBS` dirs and
  not in `SKIP`, **plus** an explicit one-entry allowlist
  `DESKTOP_EXTRA = ['ai-hub/ai-hub.html']`.

Define `DESKTOP_EXTRA` as a named const with a comment explaining it is the
launcher-grid exception. Do NOT widen it. Note the trap this rule avoids: a
naive "under a hub dir" rule would also pull in the other two SKIP entries
(`security-hub/threat-shield-blocked.html`, `security-hub/lookalike-warning.html`)
— they are interstitial warning pages, not launchable tools, and must appear in
NEITHER view. If your run produces 193 desktop entries instead of 191, that is
the bug.

**Add the missing removal path.** An entry whose `path` resolves to no file on
disk gets pruned. This is the actual drift fix. Print each pruned id.

**Add a `--check` mode.** Regenerate in memory, compare against what is
committed on disk, and `process.exit(1)` with a diff summary on any mismatch.
`--check` must write NOTHING. Default (no flag) keeps today's write behaviour.

### 4. `package.json` (edit — one new script key only)

Add `"catalog:check": "node scripts/sync-tools-catalog.cjs --check"`.

Do NOT change the `version` field. It is currently out of sync with
`manifest.json` on purpose — another open PR owns that fix and touching it here
guarantees a merge conflict. Do NOT restructure the `test` or `test:shard`
scripts; leave them exactly as they are.

### 5. `scripts/preflight.cjs` (edit)

Add ONE gate that shells out to `catalog:check`, following the existing
`if (!ok) { ... } else console.log('✓ ...')` pattern used by the other ~12 gates
in that file. Label it `✓ tool catalog in sync (canonical + browser view)`.

### 6. `tests/regression/catalog-drift.test.js` (new)

**This repo has a static-only test ratchet: a new test file whose assertions are
mostly regexes over a module's SOURCE TEXT while never executing that module
FAILS CI.** So your test must EXECUTE the generator, not grep it. Use
`child_process.execFileSync('node', ['scripts/sync-tools-catalog.cjs', '--check'])`
and assert it exits 0. Also assert, by parsing `catalog/catalog.json`:
199 tools, ids unique, every `surfaces` array non-empty, exactly 198 entries
carrying `browser`, exactly 191 carrying `desktop`, and that
`ai-hub/ai-hub.html` has `desktop` but NOT `browser`. Recompute the hash and
assert it matches the committed `hash` field.

### 7. `sidepanel/tools-catalog.json` (must be UNCHANGED)

This is the proof the migration is a no-op. After running the generator,
`git diff --stat sidepanel/tools-catalog.json` must print nothing. If it does
not, your browser-view derivation is wrong — fix the derivation, do not "fix"
the catalog.

## Do NOT do these

- Do NOT migrate the ~8 existing consumers of `sidepanel/tools-catalog.json`.
  It stays in place, at its current shape, as a generated view. Blast radius:
  this PR must change zero consumers.
- Do NOT add, rename, or recategorise any tool entry.
- Do NOT add a new hub directory or hub page.
- Do NOT touch `manifest.json`, `package.json`'s `version`, the `test` scripts,
  or anything under `background/modules/`.
- Do NOT add a GitHub Actions workflow file. `preflight` is the gate.

## Verification — run these and paste the REAL output into the PR description

    node scripts/sync-tools-catalog.cjs
    git diff --stat sidepanel/tools-catalog.json          # must be EMPTY
    node -e "const c=require('./catalog/catalog.json');console.log('tools',c.tools.length,'browser',c.tools.filter(t=>t.surfaces.includes('browser')).length,'desktop',c.tools.filter(t=>t.surfaces.includes('desktop')).length)"
    npm run catalog:check                                  # must exit 0
    node --test --test-force-exit tests/regression/catalog-drift.test.js
    npm run preflight

Expected: `tools 199 browser 198 desktop 191`.

**Prove the gate actually bites (this is an acceptance criterion — paste both runs):**
deliberately corrupt the committed catalog (e.g. delete one entry from
`catalog/catalog.json`), run `npm run catalog:check`, and show it exits NON-ZERO
with a useful message. Then restore the file and show it exits 0 again. A gate
you have not seen fail is not a gate.

**About `npm run preflight`:** on current main it already FAILS with exactly one
pre-existing error — `Version mismatch: manifest.json is 1.4.14, package.json is
1.4.13` — plus some warnings. That error is NOT yours and another open PR owns
it. Your bar is: preflight reports that SAME single pre-existing error and no
new ones, and your `✓ tool catalog in sync` line is present and passing. State
this explicitly in the PR description so the reviewer is not misled.

## PR description must also contain

The 198-vs-203 reconciliation as a table: the 12 dead desktop paths (pruned),
the 4 browser-only paths, the 4 external-URL entries, and the 1 desktop-only
live entry (`ai-hub/ai-hub.html`) — each with the reason it falls where it does.
Do not silently take one side; account for every one of the 21 differences.

Also document the distribution decision in the PR body: **vendored file + hash**
(the desktop plugin must keep working when the Toolbelt checkout is absent, so a
read-through from `extension.checkout` is rejected).

Title the PR: "feat(catalog): one generated canonical tool catalog + drift gate (TIM-17)".
